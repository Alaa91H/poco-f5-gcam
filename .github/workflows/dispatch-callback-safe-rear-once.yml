name: Dispatch callback-safe rear build once

on:
  push:
    paths:
      - ".github/workflows/dispatch-callback-safe-rear-once.yml"

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch standalone Pixel Camera build
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh workflow run build.yml \
            --repo "$GITHUB_REPOSITORY" \
            --ref main \
            -f build_experimental_standalone=true
