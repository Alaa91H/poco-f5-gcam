# Lens Mapping Captures

This directory contains manually verified POCO F5 physical lens mappings.

A valid capture uses a live Camera2 preview and physical lens occlusion to confirm which lens corresponds to each exposed Camera ID.

Do not infer lens roles from Camera ID numbers alone.